type Query {
    groups: [Group!]!
}

interface FilteredUsers {
    users(matching: String): [User!]!
}

type Group implements FilteredUsers {
    name: String!
    users(matching: String): [User!]!
    foobar(times: Int): String!
}

type User {
    name: String!
}
